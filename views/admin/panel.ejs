<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panel Administracyjny</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/bootstrap/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
</head>
<body>
    <%- include('../partials/header', { type: 'admin', currentPath: '/admin/' }) %>

    <div class="container-fluid">
        <div class="row">
            <%- include('../partials/sidebar', { currentPath: 'dashboard' }) %> 

            <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
                <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                    <h1 class="h2">Panel Administracyjny</h1>
                </div>

                <p>Witaj, <strong><%= username %></strong>! Wybierz opcję z menu po lewej stronie, aby rozpocząć zarządzanie.</p>
            </main>
        </div>
    </div>

    <%- include('../partials/footer', { type: 'admin' }) %>
</body>
</html>
