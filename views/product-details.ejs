<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Szczegóły produktu</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/bootstrap/bootstrap.min.css">
</head>
<body>
    <%- include('partials/header') %>
    
    <main class="container my-5">
        <h1 class="text-center mb-4">Szczegóły produktu</h1>

        <div class="row">
            <div class="col-md-6">
                <img 
                    src="/uploads/<%= product.imageUrls[0] %>" 
                    class="img-fluid" 
                    alt="<%= product.name %>" 
                    style="object-fit: cover; max-height: 400px;">
            </div>
            <div class="col-md-6">
                <h3><%= product.name %></h3>
                <p><strong>Cena:</strong> <%= product.price %> PLN</p>
                <p><strong>Opis:</strong> <%= product.description %></p>
                
                <form action="/products/add" method="POST" class="d-flex">
                    <input type="hidden" name="id" value="<%= product.id %>">
                    <input type="hidden" name="name" value="<%= product.name %>">
                    <input type="hidden" name="price" value="<%= product.price %>">
                    <input type="hidden" name="searchQuery" value="<%= searchQuery || '' %>">
                    <div class="input-group w-auto">
                        <input type="number" name="quantity" value="1" min="1" required class="form-control" style="width: 70px;">
                    </div>
                    <button type="submit" class="btn btn-success ms-2">Dodaj do koszyka</button>
                </form>
            </div>
        </div>

        <div class="text-center mt-4">
            <a href="/products?search=<%= searchQuery || '' %>" class="btn btn-primary">Powrót do produktów</a>
        </div>
    </main>

    <%- include('partials/footer') %>
</body>
</html>
